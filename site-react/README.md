# Building

Install Rust using the install script from the site.

Run ``pnpm install`` and ``cargo install cargo-watch``

If you get an error while building:
On Ubuntu, install build-essentials. On Windows, install MVSC's C++ build tools

# Running

Running ``pnpm dev`` will run the project at http://localhost:5000
